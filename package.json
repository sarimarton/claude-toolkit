{
  "name": "claude-toolkit",
  "version": "0.1.0",
  "description": "Modular session management toolkit for Claude Code",
  "type": "module",
  "bin": {
    "claude-toolkit": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc && cp -r src/modules/*/assets dist/modules/ 2>/dev/null; for d in src/modules/*/; do m=$(basename $d); mkdir -p dist/modules/$m && cp -r $d/assets dist/modules/$m/ 2>/dev/null; done",
    "dev": "tsx src/cli.tsx",
    "start": "node dist/cli.js"
  },
  "dependencies": {
    "ink": "^5.1.0",
    "ink-select-input": "^6.0.0",
    "ink-spinner": "^5.0.0",
    "ink-text-input": "^6.0.0",
    "meow": "^13.0.0",
    "react": "^18.3.1",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  }
}
